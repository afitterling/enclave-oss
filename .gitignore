# infra
infra/node_modules/
infra/sst-env.d.ts

# SST state (pulumi state, stack outputs, secrets) — never commit, any depth
.sst/

# web
web/node_modules/
web/dist/

# python
cli/build/
cli/*.egg-info/
cli/**/__pycache__/
.venv/

# local config / secrets — never commit
.env
.env.*
**/.enclave/
